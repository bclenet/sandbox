name: pull_request

on: pull_request

jobs:
  pr-job:
    runs-on: ubuntu-latest
    steps:
    - name: Say Hi
      run: echo "Run by pull_request"

  message-job:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: mshick/add-pr-comment@v2
        with:
          message: |
            Type your message here
